*qf-formatter.nvim.txt*     For NVIM v0.10.0     Last change: 2026 February 21

==============================================================================
Table of Contents                        *qf-formatter.nvim-table-of-contents*

1. qf-formatter.nvim                     |qf-formatter.nvim-qf-formatter.nvim|
  - 📋 Requirements  |qf-formatter.nvim-qf-formatter.nvim-📋-requirements|
  - 🛠️ Installation|qf-formatter.nvim-qf-formatter.nvim-���-️-installation|
  - 💻 Features          |qf-formatter.nvim-qf-formatter.nvim-💻-features|
  - 🔧 Configuration|qf-formatter.nvim-qf-formatter.nvim-🔧-configuration|
  - 🔧 Development    |qf-formatter.nvim-qf-formatter.nvim-🔧-development|

==============================================================================
1. qf-formatter.nvim                     *qf-formatter.nvim-qf-formatter.nvim*

Beautiful quickfix and location list formatting with diagnostic and LSP kind
icons.


П�� REQUIREMENTS      *qf-formatter.nvim-qf-formatter.nvim-📋-requirements*

- **Neovim 0.10+**
- **Nerd Font** for icon rendering


П��ϸ� INSTALLATION*qf-formatter.nvim-qf-formatter.nvim-���-️-installation*

Install via your preferred plugin manager. The following example uses lazy.nvim
<https://github.com/folke/lazy.nvim>.

>lua
    {
      'wassimk/qf-formatter.nvim',
      event = 'VeryLazy',
      opts = {}
    }
<


П�� FEATURES              *qf-formatter.nvim-qf-formatter.nvim-💻-features*

- Formatted quickfix and location list entries: `filename │line:col│ [icon] text`
- Truncates long filenames with ellipsis
- Replaces `$HOME` with `~` in file paths
- Diagnostic icons for error, warning, info, and hint entries
- LSP kind icons (codicons) for document symbol entries
- Syntax highlighting for filenames, separators, line numbers, diagnostics, and LSP kinds


П�� CONFIGURATION    *qf-formatter.nvim-qf-formatter.nvim-🔧-configuration*

The `setup()` function accepts an options table. All values are optional.

>lua
    require('qf-formatter').setup({
      filename_width = 31,     -- max filename column width (default: 31)
      kind_hl_prefix = nil,    -- nil = auto-detect, string = use prefix, false = disable
    })
<


LSP KIND HIGHLIGHT AUTO-DETECTION ~

By default (`kind_hl_prefix = nil`), the plugin auto-detects which completion
plugin provides highlight groups at the time the quickfix window opens. This
handles lazy-loaded completion plugins correctly.

Detection order:

1. **blink.cmp** — checks for `BlinkCmpKindMethod` highlight group
2. **nvim-cmp** — checks for `CmpItemKindMethod` highlight group
3. If neither is found, LSP kind highlighting is silently skipped

You can override auto-detection:

>lua
    -- Use a specific highlight prefix
    require('qf-formatter').setup({
      kind_hl_prefix = 'BlinkCmpKind',
    })
    
    -- Disable LSP kind highlighting entirely
    require('qf-formatter').setup({
      kind_hl_prefix = false,
    })
<


П�� DEVELOPMENT        *qf-formatter.nvim-qf-formatter.nvim-🔧-development*

Run tests and lint:

>shell
    make test
    make lint
<

Enable the local git hooks (one-time setup):

>shell
    git config core.hooksPath .githooks
<

This activates a pre-commit hook that auto-generates
`doc/qf-formatter.nvim.txt` from `README.md` whenever the README is staged.
Requires pandoc <https://pandoc.org/installing.html>.

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
